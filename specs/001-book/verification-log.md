# Verification Log: Textbook Content ("book")

**Feature**: `001-book` | **Created**: 2026-08-10 | **Plan**: [plan.md](./plan.md) (decision D5)

Execution status of every code example in the book. One row per example.

**This file is repo-side only.** It is authoring scaffolding and must never appear in, or be
referenced by, reader-facing content (plan D5, FR-020).

---

## Why this exists

Constitution Principle II requires every code example to be executed and to produce the stated
result before its chapter is complete. The development machine is Windows with no ROS 2, Gazebo,
or Isaac installed, so most examples cannot be executed at authoring time.

Rather than let that gap resolve silently, "done" is split in two:

| State | Meaning |
|---|---|
| `drafted` | Written, reviewed by both agents, all blockers fixed, site builds. Code **not** executed. |
| `verified` | Every example executed in the target environment, producing the stated result. |

**A chapter is `verified` only when every one of its rows below reads `executed`. Only `verified`
is done.** Reporting a `drafted` chapter as complete is a defect.

The Module 2 review (T017a) is the strongest evidence yet for why this split exists. Static
verification against upstream source caught a **blocker** — Chapter 2.2's `fall_probe.py` could
not have printed a single line of its documented output — that no amount of re-reading the prose
would have found, and that one execution would have exposed in ten seconds. See the Module 2
findings section below.

---

## Status values

| Status | Meaning | Resolution |
|---|---|---|
| `executed` | Run by the author; produced the stated result | none needed |
| `pending-env` | Not run; the environment exists but is not yet stood up | task T037 |
| `blocked` | Not run; the required environment is **not available at all** | needs a hardware or access decision |

`pending-env` and `blocked` are deliberately different. `pending-env` has a scheduled resolution.
`blocked` does not — it needs someone to decide something.

## Environments

| Key | What it is | Covers |
|---|---|---|
| `local-python` | Python on the authoring machine | plain-Python examples with no ROS dependency |
| `ros:jazzy-desktop` | `osrf/ros:jazzy-desktop` Docker image under WSL2 (research R2, preferred) | Module 1 fully; headless Gazebo for most of Module 2 |
| `ubuntu-24.04-gui` | Native or VM Ubuntu 24.04 with a display | Gazebo GUI (Modules 2.1, 2.2) |
| `ubuntu-24.04-gpu` | Ubuntu 24.04 with a display **and working GPU rendering** (OGRE 2) | Chapter 2.3's LiDAR and depth camera; Chapter 2.4's Unity Editor. Added at T017a — the module review established that software rendering is **not** sufficient for these, correcting a landing-page claim that it was |
| `rtx-gpu` | Ubuntu 24.04 + RTX-class NVIDIA GPU, CUDA 13.0+, driver 580+ | Module 3 only. **No software substitute** |

---

## Log

Target platform for every example: **ROS 2 Jazzy Jalisco · Ubuntu 24.04 (Noble) · Gazebo Harmonic**
(spec Assumption 2).

| Chapter | Example | Status | Environment required | Date checked | Note |
|---|---|---|---|---|---|
| 1.1 Introduction to Physical AI & Embodied Intelligence | `sense_think_act.py` — plain-Python sense–think–act loop | `executed` | `local-python` | 2026-08-10 | Run on Python 3.14.2 (Windows), three times independently: by the writer, by `code-verifier`, and by the orchestrator after the fix pass. Code block extracted programmatically from the `.mdx` each time and diffed byte-for-byte against the chapter's stated output: identical. Deterministic across repeated runs. Seeded with `random.Random(7)`; noise drawn from `random()` only, which the standard library guarantees reproducible across versions. Source parses under the 3.8 grammar (`ast.parse(feature_version=(3, 8))`), supporting the chapter's "Python 3.8 or newer" claim. All four exercises executed as variants; `code-verifier` corrected two overstated self-checks (seeds 1 and 3 give identical results; the proportional-policy run is 3.46× the cycles, not 3×) and one misattribution in the walkthrough (the −0.010 m stopping error is discretization from the 0.03 m creep step, not sensor noise — confirmed by re-running with the noise term zeroed). |
| 1.2 ROS 2 Architecture: Nodes, Topics, Services | `range_sensor.py` + `pilot.py` — talker/listener pair over the `/gap` topic | `pending-env` | `ros:jazzy-desktop` | 2026-08-10 | **Not executed** — no ROS 2 on the authoring machine. Static verification only: `ast.parse` + `py_compile` clean, `pyflakes` zero findings, Jazzy init/shutdown idiom confirmed (no context-manager form, no `ExternalShutdownException`, no `AsyncNode`). `example_interfaces/msg/Float64` confirmed to exist on the `jazzy` branch; both ends agree on topic name, type and queue depth. Structurally matches `ros2/examples@jazzy` `publisher_member_function.py` / `subscriber_member_function.py`. **Unconfirmed until run**: the two console-output blocks (log-line format and timestamp shape were confirmed against `rcutils@jazzy` `logging.c`/`time.c`, but the actual emitted lines were never observed). |
| 1.2 ROS 2 Architecture: Nodes, Topics, Services | `motion_gate.py` + `enable_motion_client.py` — service server/client on `/enable_motion` | `pending-env` | `ros:jazzy-desktop` | 2026-08-10 | **Not executed** — no ROS 2 on the authoring machine. Static verification only: parse clean, `pyflakes` zero findings, Jazzy idiom confirmed. `std_srvs/srv/SetBool` confirmed in `ros2/common_interfaces@jazzy` and correctly attributed to `std_srvs`. Client pattern (`rclpy.create_node` → `create_client` → `wait_for_service` → `call_async` → `spin_until_future_complete`) matches `ros2/examples@jazzy` `minimal_client` line-for-line in structure; `(request, response)` callback contract confirmed. **Unconfirmed until run**: the emitted log lines. |
| 1.2 ROS 2 Architecture: Nodes, Topics, Services | CLI inspection sequence — `ros2 node list`, `topic list -t`, `topic info`, `topic echo`, `interface show`, `service list`, `service type`, `service call` | `pending-env` | `ros:jazzy-desktop` | 2026-08-10 | **Not executed.** Output *shapes* confirmed against `ros2cli@jazzy` source (`ros2topic/verb/info.py`, `list.py`, `ros2service/verb/call.py`, `ros2interface/verb/show.py`). Both `ros2 interface show` blocks are character-exact reproductions of `example_interfaces/msg/Float64.msg` and `std_srvs/srv/SetBool.srv` on the `jazzy` branch, comments included. Review corrected the `ros2 service list` block: an `rclpy` node on Jazzy also starts `~/get_type_description` (REP 2016, default-on per `rclpy@jazzy` `type_description_service.py`), so eight lines, not seven — the official turtlesim listing shows six because turtlesim is `rclcpp`. **Re-check at T037**: (a) that `get_type_description` does appear as inferred from source rather than observed; (b) the float rendering of `ros2 topic echo` YAML (`data: 0.86`), inferred from `rosidl_runtime_py.message_to_yaml` + PyYAML; (c) `topic list`/`service list` ordering, which the CLI does not sort. |
| 1.3 Bridging Python Agents with rclpy | `pilot.py` — agent node: subscribes `/gap`, decides via plain-Python `choose_speed`, publishes `/cmd_speed`; two parameters and a status timer | `pending-env` | `ros:jazzy-desktop` | 2026-08-10 | **Not executed** — no ROS 2 on the authoring machine. Static verification only: `ast.parse` clean, `pyflakes` zero findings, Jazzy idiom confirmed. **This is the agent-node skeleton every later module reuses (tasks T009).** Skeleton confirmed byte-for-byte against the canonical `ros2/examples@jazzy` `publisher_member_function.py` / `subscriber_member_function.py`. 26 APIs confirmed against `jazzy`-branch source, including `declare_parameter` type inference (`node.py`), rejection of a wrong-typed `set` (`SetParametersResult(successful=False)`), `get_parameter().value`, and `logger.warning` (not the deprecated `warn`). Interop with 1.2's `range_sensor.py` exact: topic `gap`, type `example_interfaces/msg/Float64`, depth 10 both ends. Review corrected a **wrong prose claim** that the shutdown pair is reached on `Ctrl+C` — rclpy's SIGINT handler chains to CPython's, so `KeyboardInterrupt` propagates out of `spin` and the cleanup lines are skipped (`rclpy/src/rclpy/signal_handler.cpp`; zero `KeyboardInterrupt` handling in jazzy `rclpy`). The code was correct; only the prose was wrong. **Unconfirmed until run**: the emitted log lines. |
| 1.3 Bridging Python Agents with rclpy | `blocking_agent.py` — demonstrates a slow callback stalling the node | `pending-env` | `ros:jazzy-desktop` | 2026-08-10 | **Not executed.** Parse clean, `pyflakes` zero findings, Jazzy idiom confirmed. The chapter's central mechanism claims were confirmed against source: timer ticks are **skipped, not deferred** (`ros2/rcl@jazzy` `src/rcl/timer.c` — `next_call_time += period`, then advance by whole periods while `next_call_time <= now`, so a 1 Hz timer blocked 2 s fires once); default queue depth 10 with keep-last discard (Jazzy QoS docs); no exception or warning raised during a stall. Callback interleaving order (`done` → `heartbeat` → `start`) confirmed against `rclpy@jazzy` `executors.py` wait-order (waitables → timers → subscriptions). **Unconfirmed until run**: the emitted log lines and their timestamps. |
| 1.3 Bridging Python Agents with rclpy | CLI sequence — `ros2 param list` / `get` / `set`, `ros2 topic echo /cmd_speed`, `--ros-args -p` | `pending-env` | `ros:jazzy-desktop` | 2026-08-10 | **Not executed.** Exact output strings confirmed against `ros2cli@jazzy`: `ros2 param list` two-space indent and sorted (`list.py`), `Double value is:` (`get.py`), `Set parameter successful` (`set.py`), `average rate: %.3f` for `ros2 topic hz` (`hz.py`). `use_sim_time` auto-declared on every node (`time_source.py`). `--ros-args -p name:=value` confirmed in `ros2_documentation@jazzy` `Node-arguments.rst`. **Closes an item left inferred at 1.2**: `ros2 topic echo` float rendering is now confirmed from `echo.py` + `message_to_yaml` — `data: 0.3` / `data: 0.0` with `---` and no blank line. All five transcripts in this chapter were re-derived arithmetically and are mutually consistent (staleness 4.50 s, real gap 0.56 m, reading counts, and the sweep wrap all check out). |
| 1.4 URDF: Describing a Humanoid | Example 1 — `simple_humanoid.urdf`, the complete 13-link humanoid model | `executed` | `local-python` | 2026-08-11 | **The single humanoid model required by FR-017.** Extracted programmatically from the `.mdx` and parsed with `xml.etree.ElementTree` three times independently: by `code-verifier`, by `chapter-writer` after the fix pass, and by the orchestrator against the final shipped file. Structure confirmed each time: well-formed XML, `robot name="simple_humanoid"`, 13 links, 12 joints, exactly one root (`base_link`), no duplicate link or joint names, every joint parent/child resolving to a declared link, no cycles, all 13 links reachable. All 12 `revolute` joints carry `<limit>` with `effort` and `velocity` (urdfdom rejects the file otherwise) and non-degenerate `lower < upper`; every joint has an explicit `<axis>`; all three `<material>` names declared and used. **All 39 inertia components recomputed** from mass and shape against the standard rigid-body formulas and correct to the printed precision, including the head's deliberate use of the visual sphere radius 0.11 (`2·2.5·0.11²/5 = 0.0121`) rather than the collision 0.12 (which would give 0.0144). Every numeric prose claim re-derived independently: total mass 34.700 kg, skull top +0.720 m, soles −0.850 m, height 1.5700 m; Exercise 3's `l_hand` inertias (0.000453 / 0.000333 / 0.000547) and totals; Exercise 4's visor corners, the 1.01 cm and 1.31 cm clearances and the 0.133 m enclosing radius. The three inline XML excerpts were confirmed byte-identical to their counterparts in the full listing. **Not executed**: the official `urdfdom` parse, RViz rendering, and any physics engine — logged as separate rows below. |
| 1.4 URDF: Describing a Humanoid | Example 2 — `urdf_tree.py`, stdlib joint-tree walker + its printed transcript | `executed` | `local-python` | 2026-08-11 | **Run on Python 3.14.2 (Windows), four independent times**: by `code-verifier`, by `chapter-writer` post-fix, and by the orchestrator against the final shipped `.mdx`. Exit 0; stdout matches the chapter's ` ```text ` transcript exactly once line endings are normalized (the only difference is Windows CRLF from `print`, not content — verified byte-wise). Standard library only (`sys`, `xml.etree.ElementTree`, `%`-formatting); nothing in it postdates Python 3.7, supporting the "Python 3.8 or newer" claim, though Ubuntu 24.04 ships 3.12 and only 3.14.2 was available here. All three exercise mutations were also executed and reproduce their stated results exactly, including the literal string `not a tree: expected 1 root link, found 2: ['base_link', 'l_shin']` and the `reached: 11` against `links: 13` pair. Review corrected a **false prose claim** that "three checks run first": only two run before the walk and return 1, while `reached` is computed during the walk, printed last, and never compared — a broken tree exits 0. The code was correct; the prose was wrong, and was reworded rather than changing the code, preserving Exercise 2's pedagogy and the executed transcript. Determinism confirmed: `findall` yields document order and dicts preserve insertion order. |
| 1.4 URDF: Describing a Humanoid | `check_urdf simple_humanoid.urdf` — validation with the official parser | `pending-env` | `ros:jazzy-desktop` | 2026-08-11 | **Not executed** — needs Ubuntu 24.04 with `sudo apt install liburdfdom-tools` (independent of ROS; `liburdfdom-tools` 4.0.0-0ubuntu1 on Noble installs `/usr/bin/check_urdf`, confirmed from the packages.ubuntu.com filelist). Output *shape* confirmed from `check_urdf.cpp`: `robot name is: `, the `Successfully Parsed XML` banner, and `root Link: X has N child(ren)` with two-space indented `child(n)` lines. Both quoted failure strings were corrected this pass to match `urdfdom_headers@jazzy` `model.h` exactly — `"Two root links found: [name] and [name]"` (the chapter previously quoted only the prefix) and `"No root link found. The robot xml is not a valid tree."` (trailing period restored). Exercise 2's premise confirmed from source: `initRoot` has no reachability or cycle check, so the delete is rejected while the reparent is accepted silently — which is the exercise's point. **Unconfirmed until run**: the actual emitted banner and tree listing. |
| 1.4 URDF: Describing a Humanoid | `display_humanoid.launch.py` + `ros2 launch` + the RViz walkthrough | `pending-env` | `ubuntu-24.04-gui` | 2026-08-11 | **Not executed** — needs ROS 2 Jazzy on Ubuntu 24.04 **with a display**. `py_compile` clean. All four launch APIs confirmed real and correctly pathed on the `jazzy` branches: `LaunchDescription` (`launch/__init__.py`), `Command` (`launch/substitutions/__init__.py`), `Node` (`launch_ros/actions/__init__.py`), `ParameterValue` (`launch_ros/parameter_descriptions.py`), whose signature makes `value_type` keyword-only — `value_type=str` is load-bearing, since without it the URDF is YAML-parsed and fails. `ros2 launch` with a relative path outside a package confirmed via `ros2launch/command/launch.py`'s single-file branch. Package/executable pairs confirmed for all three nodes. `robot_state_publisher` confirmed to publish the URDF on a `robot_description` topic with `transient_local` QoS, which is how the GUI and RViz obtain the model and why launch ordering does not matter. Two fixes this pass: the claim that all three packages ship in `ros-jazzy-desktop` was **factually wrong** (`joint_state_publisher` appears in none of the six `ros2/variants@jazzy` variants) and was corrected; and the `/home/you/` placeholder path was replaced with a self-locating `os.path.realpath(__file__)` form to satisfy the spec's no-placeholder-values criterion. **Note**: replacing the launch file with `ros2 run --ros-args -p robot_description:="$(cat …)"` was investigated and **refuted** — libyaml rejects the multi-line XML — so the launch file was kept and explained rather than removed. **Unconfirmed until run**: that the three nodes come up, the sliders move the model, and the RViz default-config claims (grid-only, `/map` Fixed Frame). |
| 2.1 Gazebo Setup & Simulation Basics | Example 1 — `gz sim -v 4 empty.sdf`, `ros2 run ros_gz_sim create -world empty -file … -z 0.9`, `gz topic -l` / `-e -t /clock -n 1`, `ros2 topic list`, `parameter_bridge /clock@rosgraph_msgs/msg/Clock[gz.msgs.Clock`, `ros2 topic info /clock` | `pending-env` | `ubuntu-24.04-gui` | 2026-08-15 | **Backfilled at T017a** — chapter 2.1 was drafted and committed (`02c2e9b`) with no rows here at all; this is the gap the previous revision of this file flagged. **Not executed** — needs a display and OpenGL for the GUI, and the example turns on pressing Play. Static verification against upstream source confirmed, this pass: `ros-jazzy-ros-gz` as a metapackage pulling `ros_gz_sim` / `ros_gz_bridge` / `ros_gz_image` plus Gazebo itself as vendor packages (`package.xml` on `jazzy`, gazebosim.org `harmonic/ros_installation`); `gz sim` verbosity range 0–4 with default 1, and the `-r` / `-s` flags (`gz-sim8` `cmdsim.rb.in`); default-paused start; every `create` flag (`-world`, `-file`, `-name`, `-z`), the `/world/<name>/create` service, the omit-`-world`-and-take-the-first behaviour, and the success line `[ros_gz_sim]: Entity creation successful.` **character-for-character** including the `ros_gz_sim` node name (`ros_gz_sim/src/create.cpp`); the `@` / `[` / `]` direction table exactly as printed (`parameter_bridge.cpp` usage text); the `/clock` bidirectional caveat including Gazebo's "Found additional publishers on /clock" fallback to the namespaced topic (`SimulationRunner.cc`); that clock and stats are published on **every loop pass including while paused** (`SimulationRunner::Step` calls `PublishStats()` unconditionally); `empty.sdf`'s contents — sun, 100 m `ground_plane`, physics, exactly four systems, `max_step_size` 0.001, world named `empty`; Gazebo Classic EOL 29 Jan 2025 and Harmonic EOL May 2029 matching Jazzy; libsdformat's URDF parser converting `<robot>` roots. Soles at −0.85 m re-derived from the Chapter 1.4 URDF. **Fixed at T017a**: the chapter's three labelled examples were merged to two (FR-007 allows one or two), and the authoring-process disclaimers were removed to `verification-log.md` where status belongs (plan D5, NFR-002). **Unconfirmed until run**: every console line, the GUI behaviour, and that the humanoid visibly collapses on Play. |
| 2.1 Gazebo Setup & Simulation Basics | Example 2 — `sim_link_check.py`, rclpy node subscribing the bridged `/clock` and reporting messages, sim-vs-wall elapsed, RTF and node clock | `pending-env` | `ubuntu-24.04-gui` | 2026-08-15 | **Backfilled at T017a. Not executed** — needs a running `gz sim` and a live `/clock` bridge. Block extracted programmatically and `python -m py_compile` clean (rc 0). Jazzy rclpy idiom confirmed — no context-manager `init`, no `ExternalShutdownException`, no `AsyncNode`; follows Chapter 1.3's node skeleton. `rosgraph_msgs/msg/Clock` confirmed real and present in any ROS 2 install; `use_sim_time` auto-declared by every rclpy node (`rclpy@jazzy` `node.py` → `TimeSource`). **Two majors found and fixed this pass, both the same root cause**: the report timer was created with `create_timer(REPORT_PERIOD, self.on_report)`, which falls back to the node's ROS clock (`node.py`: `if clock is None: clock = self._clock`). Under `use_sim_time:=true` that clock stops advancing whenever sim time freezes, so (a) Exercise 1's instruction to pause Gazebo and "watch three report lines" would have produced **no output at all**, and (b) the `no /clock messages yet` warning the chapter tells the reader to look for when the bridge is down was **unreachable**, since with no `/clock` publisher the ROS clock never advances and the timer never fires. Fixed by giving the timer an explicit steady clock (`rclpy.clock.Clock(clock_type=rclpy.clock.ClockType.STEADY_TIME)` passed as `clock=`), which also matches the node's stated purpose of measuring simulated time *against* wall time; `create_timer`'s `clock=` keyword was confirmed present in the Jazzy signature before the change was made. Exercise 1 was rewritten to match the corrected behaviour, and a new exercise step has the reader delete the argument to observe the original failure deliberately. **Also fixed**: the RTF figure is a cumulative average since startup while Gazebo's status bar shows a smoothed instantaneous value — the chapter had called them "the same quantity", which is true only in steady state. **Unconfirmed until run**: every console line and the pause/play behaviour. |
| 2.1 Gazebo Setup & Simulation Basics | Exercises 1–4 — pause/play clock divergence, removing `use_sim_time`, killing the bridge, bridging the humanoid's poses | `pending-env` | `ubuntu-24.04-gui` | 2026-08-15 | **Backfilled at T017a. Not executed.** Exercises 1–3 depend on the timer fix above and their stated results were re-derived against the corrected code. **Exercise 4 carried a major and was rewritten**: it had the reader bridge `/world/empty/pose/info` as `tf2_msgs/msg/TFMessage` and claimed `ros2 topic echo` would show "one transform per link". The *count* is right but the transforms are **anonymous** — `SceneBroadcaster` sets only `name` and `id` per pose and puts a header on the enclosing `Pose_V` only, while `ros_gz`'s `Pose → TransformStamped` converter reads `child_frame_id` exclusively from per-pose `header().data()` key-value pairs and never looks at `pose.name()`. Every transform therefore arrives with empty `frame_id`, empty `child_frame_id` and a zero stamp. Both halves of this were verified independently by the orchestrator against `gz-sim8` `SceneBroadcaster.cc` and `ros_gz@jazzy` `convert/geometry_msgs.cpp`. The exercise now says plainly that `pose/info` is a diagnostic rather than a TF source, which also sets up Chapter 2.2's use of the pose publisher. **Unconfirmed until run**: all four observed results. |
| 2.2 Physics, Gravity & Collisions | Example 1 — `fall_probe.py` + a `gz-sim-pose-publisher-system` `<gazebo>` block on the humanoid, sampling model world-z and head-z-relative-to-model to distinguish toppling from collapsing | `pending-env` | `ubuntu-24.04-gui` | 2026-08-15 | **Not executed. This row carried the module's one blocker, which is now CLOSED and independently re-verified.** As originally drafted the node subscribed `/world/empty/pose/info` and filtered on `short_name(transform.child_frame_id)`; every transform arrives with an empty `child_frame_id`, so **every message was skipped and the node could not have emitted one line of its documented output**. **Fix**: a `<gazebo>` block attaching `gz-sim-pose-publisher-system` with `publish_model_pose`, `publish_link_pose`, `use_pose_vector_msg` and `update_frequency`, and the bridge argument and `POSE_TOPIC` both pointed at `/world/empty/model/simple_humanoid/pose`. **A targeted re-review of this chapter traced the entire path in source and confirmed all ten hops**: reference-less `<gazebo>` copies its children into SDF `<model>` (`sdformat@sdf14` `parser_urdf.cc` `InsertSDFExtensionRobot`/`CopyBlob`); the advertised topic is `topicFromScopedName(...) + "/pose"` = exactly `/world/empty/model/simple_humanoid/pose` (`PosePublisher.cc:274`, `Util.cc`), so the chapter's derivation is right; `-name simple_humanoid` fixes the scoped name (`create.cpp`); `usePoseV` advertises `Pose_V` (`PosePublisher.cc:284`); `TFMessage`↔`Pose_V` is a legal pairing (`mappings.py:117`); per-pose `frame_id`/`child_frame_id` are written for **every** pose (`PosePublisher.cc:585-596`) with values `simple_humanoid` (parent `empty`) and `simple_humanoid::head` (parent `simple_humanoid`); `frame_id_gz_to_ros` rewrites `::`→`/`; so `short_name()` yields `'simple_humanoid'` and `'head'` and **both match**. **Two items previously flagged as unconfirmed are now confirmed**: (a) `PosePublisher` **does** stamp every per-pose header — `header->mutable_stamp()->CopyFrom(_stampMsg)` with `_stampMsg = convert<msgs::Time>(_info.simTime)` — so the node's entire timing basis is sound, which had been an open risk of a second defect hiding behind the first; (b) link poses are stored **relative to the parent model** for every non-canonical link (`Physics.cc` `UpdateSim`), and `head` is non-canonical while `base_link` is canonical, so the "head rel z" reading is valid and is invariant under a rigid topple exactly as the chapter claims. Chapter 1.4's 12 joints are all `revolute`, so sdformat's fixed-joint lumping never absorbs or renames `head`. `py_compile` clean. **Unconfirmed until run**: everything the node prints, and the derived head-rest constant 0.490 m, which is computed rather than observed. |
| 2.2 Physics, Gravity & Collisions | Example 2 first half — `scale_inertia.py`, stdlib script copying a URDF and scaling one link's six inertia terms by a factor | `executed` | `local-python` | 2026-08-15 | **Run against Chapter 1.4's real `simple_humanoid.urdf`, three times independently**: by `chapter-writer`, by the orchestrator at draft time, and again by `code-verifier` at T017a against the shipped `.mdx`. Both script and URDF extracted programmatically each time; `python scale_inertia.py simple_humanoid.urdf humanoid_cad_units.urdf torso 1e6` exited 0 and stdout matched the chapter's ` ```text ` transcript line-for-line on all six inertia values (`ixx = 254800`, `iyy = 208800`, `izz = 110800`, off-diagonals 0). `py_compile` clean. Standard library only (`sys`, `xml.etree.ElementTree`), same approach as 1.4's `urdf_tree.py`. The mutation is non-destructive as claimed: torso mass 12.0, inertial origin `0 0 0.21`, box `0.18 0.28 0.42` and the 34.7 kg total all unchanged. **Placeholder now removed and the transcript is fully reproducible.** The invocation was changed to `cd ~/humanoid` with relative arguments, so the script's echoed output path is `humanoid_cad_units.urdf`. Re-executed by the orchestrator from a temp directory using the chapter's exact command against the extracted Chapter 1.4 URDF: exit 0, and stdout is **byte-identical to all eight transcript lines including the final one** — previously the only line that could not match. This example now has no known caveats. |
| 2.2 Physics, Gravity & Collisions | Example 2 second half — spawning `humanoid_cad_units.urdf` (the ×10⁶ CAD-unit slip) and comparing the two settled `fall_probe.py` reports | `pending-env` | `ubuntu-24.04-gui` | 2026-08-15 | **Not executed.** This is the chapter's payoff — the wrong-inertia-tensor demonstration the Chapter Catalog asks for — and none of it has been observed. The predicted behaviour (free-fall phase identical because inertia plays no part in translation; everything after foot contact diverging) is derived from mechanism and is labelled in the chapter as a prediction to check, not as a transcript. Inherits the Example 1 blocker: until the bridge topic and the node agree, no comparison can be run at all. **Unconfirmed until run**: the entire observed contrast between the two runs. |
| 2.2 Physics, Gravity & Collisions | Exercise 4 — `physics_lab.sdf` custom world (`max_step_size` / `real_time_factor` trade-off), `gz sim -v 4 physics_lab.sdf`, `gz topic -e -t /world/physics_lab/stats` | `pending-env` | `ubuntu-24.04-gui` | 2026-08-15 | **Not launched.** XML well-formedness re-confirmed with `xml.etree.ElementTree` against the shipped `.mdx`: parses clean, root `sdf`, world name `physics_lab`. **Closed at T017a** — the three items the writer had flagged as unverified are now checked: (a) all four plugin filename/class pairs are valid on `gz-sim8` (`gz-sim-physics-system`/`gz::sim::systems::Physics`, `…-user-commands-…`/`UserCommands`, `…-scene-broadcaster-…`/`SceneBroadcaster`, `…-contact-…`/`Contact`); (b) the `type` attribute on `<physics>` **is ignored by gz-sim** — the engine comes from `--physics-engine` / `GZ_SIM_PHYSICS_ENGINE_PATH`, and Gazebo's own shipped worlds write `type="ignored"` to signal this; DART confirmed as the default, loaded as `gz-physics-dartsim-plugin`, with `gz-physics-bullet-featherstone-plugin` also shipping in `gz-physics7`. `<surface><friction><ode><mu>/<mu2>` nesting and both defaults of 1.0 re-confirmed from `sdformat@sdf14` `sdf/1.11/surface.sdf`, which is what makes the chapter's "μ = 1.0 under both feet and it still went down" argument sound. **Now corrected**: the block declares `<sdf version="1.11">` and `type="ignored"`, matching the main text. Exercise 4's stated result was also corrected — renaming the world moves the pose publisher's topic to `/world/physics_lab/model/simple_humanoid/pose`, not the `/world/physics_lab/pose/info` it previously named, and both the bridge argument and `POSE_TOPIC` must follow. `gz topic -e -t … -n 5` re-confirmed valid (`gz-transport13` `topic_main.cc`). (c) the real-time-factor figures the exercise predicts remain unverified and need a run. |
| 2.3 Simulating Sensors | Example 1 — three `<gazebo>` sensor blocks (`imu`, `gpu_lidar`, `depth_camera`) appended to a `sensor_humanoid.urdf` copy, plus `sensor_world.sdf`, `gz sim`, spawn, and `gz topic -l \| grep humanoid` | `pending-env` | `ubuntu-24.04-gpu` | 2026-08-15 | **Not executed** — the LiDAR and depth camera render through OGRE 2 and need working GPU rendering; software rendering leaves them slow or silent. XML re-parsed against the shipped `.mdx`: the three-`<gazebo>` block is a legitimate multi-root **fragment** (parses wrapped, and parses cleanly once assembled before `</robot>` as the chapter instructs); `sensor_world.sdf` parses standalone. Verified against `sdformat@sdf14` and `gz-sensors8`: all three sensor types exist in `sdf/1.11/sensor.sdf` with valid `always_on` / `update_rate` / `visualize` / `topic` / `pose` children; `bias_mean`, `bias_stddev`, `dynamic_bias_stddev`, `dynamic_bias_correlation_time` all real (`noise.sdf`). **The chapter's non-obvious two-noise-forms claim is correct and was confirmed in all three files**: lidar and camera declare their own `<noise>` with `<type>` as a **child element**, while IMU includes `noise.sdf`, where `type` is an **attribute** — and the two forms are not interchangeable. `<gz_frame_id>` confirmed real in Harmonic (`gz-sensors8` `Sensor.cc`) and correctly described as a non-spec Gazebo extension. `<gazebo reference="link">` blob insertion confirmed in `sdformat@sdf14` `parser_urdf.cc`. All plugin/class pairs valid. Geometry recomputed: LiDAR at x = 0.10 → 2.90 m, camera at x = 0.13 → 2.87 m, camera height 1.47 m. **Major found and fixed at T017a**: the expected topic listing was wrong in two ways — `camera_info` is a **sibling** of the sensor topic, not a child (`CameraSensor::AdvertiseInfo()` pops the last path segment before appending), so it is `/humanoid/camera_info` and not `/humanoid/depth/camera_info`; and `/humanoid/scan/points` was missing entirely, since `GpuLidarSensor::Load` always advertises a `PointCloudPacked` sibling alongside the `LaserScan`. Because the chapter tells the reader a mismatch means the sensors failed to load, this would have sent them chasing a fault that did not exist. Listing corrected and the sibling rule explained; `<sdf version>` corrected 1.10 → 1.11. **Unconfirmed until run**: every topic listing and all sensor output. |
| 2.3 Simulating Sensors | Example 2 — `sensor_probe.py` reading all three sensors side by side, plus the four-topic `parameter_bridge` | `pending-env` | `ubuntu-24.04-gpu` | 2026-08-15 | **Not executed.** Block extracted programmatically; `py_compile` clean (rc 0); Jazzy rclpy idiom confirmed. Every bridge pairing in the chapter's table verified against `ros_gz_bridge/README.md` on `jazzy`, **including the easily-mistyped `gz.msgs.IMU` ↔ `sensor_msgs/msg/Imu` capitalisation**: `LaserScan`, `Image`, `PointCloudPacked` ↔ `PointCloud2`, `CameraInfo` all correct. Depth pixel decoding confirmed exactly right against `ros_gz_bridge/src/convert/sensor_msgs.cpp` — `R_FLOAT32` → `"32FC1"`, `is_bigendian = false`, `step = width × 4` — and `struct.unpack_from` confirmed to work against all three buffer types rclpy may deliver (numpy `uint8` array, `array.array('B')`, `bytes`). Lidar `inf`-for-no-hit confirmed (`Lidar::Clamp` passes non-finite values through). **Fixed at T017a**: the chapter claimed `|a|` reads ≈9.8 m/s² at rest, but each acceleration axis carries `<bias_mean>0.1</bias_mean>`, giving `sqrt(0.1² + 0.1² + 9.9²) ≈ 9.90` — a 1 % offset the reader deliberately introduced, and the perfect illustration of the chapter's own `bias_mean` paragraph. Also corrected: Exercise 2's stated mechanism for the noisy-LiDAR case — `Clamp` runs **after** `ApplyNoise` and clamps every finite range into `[range_min, range_max]`, so a strongly negative draw surfaces as a hard 0.20 m reading rather than an arbitrary short one. **Unconfirmed until run**: all three sensors' console output. |
| 2.3 Simulating Sensors | Exercises 1–4 — deleting `<gz_frame_id>`, raising LiDAR noise, the camera's floor intercept, reading the IMU at rest | `pending-env` | `ubuntu-24.04-gpu` | 2026-08-15 | **Not executed.** Exercise 1's answer (frames fall back to the *sensor name*) confirmed from `gz-sensors8` `Sensor.cc`'s `else` branch. Exercise 3 recomputed independently: floor intercept `1.47 / tan(0.35)` = **4.027 m** and slant range `2.87 / cos(0.35)` = **3.055 m**, both matching the chapter's ≈4.0 and ≈3.06. Exercise 2 rewritten per the `Clamp` ordering above. Exercise 4's expected magnitude corrected to ≈9.90 per the `bias_mean` finding. **Unconfirmed until run**: all four observed results. |
| 2.4 Unity for Visualization | Example 1 — `joint_wave.py` publishing a `sensor_msgs/msg/JointState` sweep, plus the `colcon build` of `ros_tcp_endpoint` and `ros2 launch ros_tcp_endpoint endpoint.py` | `pending-env` | `ros:jazzy-desktop` | 2026-08-15 | **Not executed.** Notably, `joint_wave.py` **needs no Gazebo, no Unity and no display** — a plain ROS 2 Jazzy box runs it, which makes it the cheapest example in Module 2 to promote to `executed` at T037. Block extracted; `py_compile` clean (rc 0); Jazzy rclpy idiom confirmed; every import used; `declare_parameter`/`get_parameter` correct; `JointState` field names and types correct. **The chapter's claimed output was checked line-by-line against the code and matches exactly**: `JOINT_NAMES` puts `r_shoulder_joint` fifth (held at −1.40, inside its ±2.00 limit so the clamp is a no-op), `r_elbow_joint` sixth (−1.00 ± 0.90 → −1.90 … −0.10), `neck_joint` second (±0.40). All ROS-TCP-Endpoint facts confirmed on the `main-ros2` branch: `launch/endpoint.py` exists and is installed to `share/ros_tcp_endpoint/launch` (`setup.py`), the launch file's `ROS_IP` `0.0.0.0` and `ROS_TCP_PORT` 10000 defaults are quoted correctly, `default_server_endpoint.py` constructs `TcpServer("UnityEndpoint")` so the `/UnityEndpoint` node name in the diagnosis ladder is right, and `package.xml` declares `ament_python`. **Fixed at T017a**: the chapter assumed a colcon workspace and overlay sourcing that the book had never introduced — and that Chapter 1.2 explicitly ruled out ("no package or build step") — while its own Environment section sourced only `/opt/ros/jazzy`, leaving `ros2 launch ros_tcp_endpoint` unresolvable; the workspace is now explained at first use and both sourcing steps are named. `python3-colcon-common-extensions` added to the environment table (FR-011) — it is not pulled in by `ros-jazzy-ros-gz`. **Unconfirmed until run**: the build succeeding on Jazzy, and all console output. |
| 2.4 Unity for Visualization | Example 2 — `JointStateReceiver.cs`, the Unity side subscribing `/joint_states` and driving `ArticulationBody` targets | `pending-env` | `ubuntu-24.04-gpu` | 2026-08-15 | **Not executed in Unity** — needs the Unity Editor with ROS-TCP-Connector and URDF Importer, a real GPU, and the humanoid imported. **But it does compile**: `code-verifier` compiled it with `csc.exe` against hand-written stubs for `UnityEngine`, `Unity.Robotics.ROSTCPConnector` and `RosMessageTypes.Sensor` — **exit 0** — correcting the chapter's own claim that the script had "never compiled", which has been removed. Every API confirmed against the real repositories: namespace `Unity.Robotics.ROSTCPConnector` with `public static ROSConnection GetOrCreateInstance()`; `public void Subscribe<T>(string topic, Action<T> callback) where T : Message`, signature-compatible with the chapter's call; `RosMessageTypes.Sensor.JointStateMsg` with `public string[] name` and `public double[] position`, so `.Length` and the `(float)` cast are correct. **The radians→degrees claim is confirmed at source**, not assumed: `UrdfJointRevolute.cs` does `drive.target += deltaState * Mathf.Rad2Deg`. The joint→child-link mapping table matches `simple_humanoid.urdf` exactly. **Could not confirm**: that ROS-TCP-Endpoint's `main-ros2` branch builds on Jazzy — Unity Robotics' published ROS 2 support names Foxy, not Jazzy. The chapter states this limitation and does not claim otherwise; it is a real risk for T037 and may need a source patch or a pinned older ROS 2. |
| 2.4 Unity for Visualization | Exercises 1–4 — stopping the wave, removing the clamp, duplicating a joint mapping, and the failure-diagnosis ladder | `pending-env` | `ubuntu-24.04-gpu` | 2026-08-15 | **Not executed.** Exercises 1 and 2 were re-derived from the code and are exact: `wave_hz = 0.0` → `phase = 0` → `sin = 0` → the elbow settles at **−1.00**, not zero; and `−1.00 + 3.00·sin` spans [−4.00, +2.00] against URDF elbow limits `lower="-2.50" upper="0.00"`, so the clamp flattens at exactly **−2.500** and **0.000** and removing it publishes **+2.0** and **−4.0**. **Fixed at T017a**: Exercise 3's claimed outcome depended on `Dictionary<string, ArticulationBody>` enumeration order, which C# does not guarantee — both joints map to the same body, so `Update()` writes `xDrive.target` twice per frame and the last write wins; the text now says so rather than stating the result as certain. Exercise 4 needs no environment at all and is the strongest of the four. **Unconfirmed until run**: the three exercises that require Unity. |
| 4.1 Voice-to-Action with Whisper | Example 1 — `voice_intent.py`, pure-Python transcript→command mapper against a bounded set (`BOUNDED_COMMANDS`), confidence gate, exact-match only | `executed` | `local-python` | 2026-08-20 | **Run on Python 3.12.10 via `py -3.12`** (not the system default 3.14.2, per this module's Python 3.12 baseline), standard library only (`dataclasses`, `typing`). `python -m py_compile` clean. stdout matches the chapter's ` ```text ` transcript exactly for all eight cases: three accepted (`STOP`, `WALK_FORWARD` ×2), one rejected as `low_confidence` (0.30 < 0.50, confirming the `<` boundary — not `<=`), one rejected as `empty_transcript`, and two rejected as `out_of_vocabulary` — `'please walk forward now'` (real, well-formed phrasing not in the table) and `'turn lift'` (a one-character mis-transcription of `'turn left'`), both demonstrating the chapter's no-nearest-match argument deliberately. Deterministic: exact-string matching only, no randomness. |
| 4.1 Voice-to-Action with Whisper | Example 2 — `voice_command_node.py`, `rclpy` node loading Whisper, transcribing one audio file, deriving a confidence proxy from `avg_logprob`, and publishing `example_interfaces/msg/String` on `voice_command` (or rejecting and publishing nothing) | `pending-env` | `ros:jazzy-desktop` | 2026-08-20 | **Not executed** — needs ROS 2 Jazzy for `rclpy`/`example_interfaces` **and** `openai-whisper` + `ffmpeg` installed under Python 3.12, none of which coexist on the authoring machine (Windows, no ROS 2). `python -m py_compile` clean on both files (`voice_command_node.py`, `voice_intent.py`); import graph is: node imports `voice_intent`, which has no ROS/ML imports, matching the chapter's separation claim. API surface verified against `openai/whisper`'s `main` branch: `whisper.load_model(name)` and `model.transcribe(path)` confirmed from the README's own usage example; the three result-dict keys (`text`, `segments`, `language`) and per-segment keys (`avg_logprob`, `no_speech_prob`, `start`, `end`, `text`, among others) confirmed from `whisper/transcribe.py`'s `new_segment()` and the `transcribe()` return statement. `example_interfaces/msg/String` (field `string data`) confirmed present on the `jazzy` branch of `ros2/example_interfaces`, consistent with this book's existing use of `example_interfaces/msg/Float64` since Chapter 1.2. `rclpy.spin_once(node, executor=None, timeout_sec=None)` confirmed from `rclpy/rclpy/__init__.py` on the `jazzy` branch. Jazzy init/shutdown idiom confirmed (no context-manager form, no `ExternalShutdownException`, no `AsyncNode`). **A bounded attempt was made** to `uv pip install --python 3.12 openai-whisper` on the authoring machine per the chapter-authoring skill's allowance; `uv` resolved the dependency graph in seconds but the `torch` wheel (~116 MiB) did not finish downloading within two bounded attempts (~5 minutes combined) on this network, so the install — and therefore any real transcription — was abandoned rather than pursued further. `estimate_confidence`'s `avg_logprob`→`[0, 1]` mapping (`exp`, clamped) is this chapter's own design choice, explicitly labelled in-text as an uncalibrated proxy, not a documented Whisper API. **Unconfirmed until run**: every console line and topic-echo output shown in the chapter, which are stated as traced-by-hand predictions, not observations. |
| 4.2 Cognitive Planning with LLMs | Example 1 — `plan_validator.py`, pure-Python plan validator: JSON parse, skill-registry grounding check, per-argument type/range check, plan-length bound | `executed` | `local-python` | 2026-08-21 | **Run on Python 3.12.10 via `py -3.12`**, standard library only (`json`, `dataclasses`, `typing`). `python -m py_compile` clean. The code block was extracted programmatically from the shipped `.mdx` with a regex, re-compiled, and re-run independently of the authoring-time run; stdout of that independent run is byte-identical to the authoring-time run (`diff` clean) and to the chapter's ` ```text ` transcript (a `diff` against the extracted transcript showed only a trailing-newline artifact of the extraction regex, not a content difference). All eight cases confirmed: two accepted (`walk_to`+`pick_up`; the four-step `walk_to`/`pick_up`/`walk_to`/`place_on` decomposition of "tidy the table"), and six rejected — `ungrounded_skill` (a skill not in `SKILL_REGISTRY`), `bad_argument_type` (a string where a float was required), `argument_out_of_range` (a coordinate outside the declared ±5.0 m bound), `plan_too_long` (7 steps against `MAX_PLAN_STEPS = 6`), `malformed_json` (a truncated array, caught by `json.JSONDecodeError`), and `bad_arguments` (a required argument missing entirely). Deterministic: no randomness, no I/O beyond stdout. |
| 4.2 Cognitive Planning with LLMs | Example 2 — `cognitive_planner_node.py`, `rclpy` node calling Claude (`claude-sonnet-5`) through a forced `propose_plan` tool, validating the result with `plan_validator.validate_plan`, retrying with the rejection reason as context up to `MAX_REPLAN_ATTEMPTS = 2`, and publishing accepted steps as `example_interfaces/msg/String` on `robot_plan_step` | `pending-env` | `ros:jazzy-desktop` | 2026-08-21 | **Not executed** — needs ROS 2 Jazzy for `rclpy`/`example_interfaces`, the `anthropic` package, and live network access to the Claude API with `ANTHROPIC_API_KEY` set, none of which are available on the authoring machine (Windows, no ROS 2, and per this chapter's explicit instruction, no attempt was made to install the `anthropic` SDK or call the API). `python -m py_compile` clean on both files; import graph confirmed: `cognitive_planner_node.py` imports `plan_validator`, which has no ROS/LLM/network imports, matching the chapter's separation claim. Tool-use API shape — `tools` with `name`/`description`/`input_schema`, `tool_choice={"type": "tool", "name": ...}` to force a specific tool, and reading a `tool_use` content block's `.type`/`.name`/`.input` off `response.content` — verified against Anthropic's own documentation (`platform.claude.com`, "Tool use with Claude" and "Define tools" pages, fetched 2026-08-21), not assumed. **Defect found and fixed at orchestration time**: the drafted tool definition carried `strict: True`, which requires every object in the schema to declare `additionalProperties: false` — the schema declared it nowhere, and its `args` field is an open per-skill map that cannot declare it at all, so the documented request would have been rejected by the API. `strict` was removed and the chapter's prose rewritten to explain why this schema design and strict mode are incompatible. Caught by checking the tool definition against the strict-mode requirement in the `claude-api` skill, which the drafting agent could not load. Model ID `claude-sonnet-5` confirmed current (non-deprecated) from the same source's pricing table. `example_interfaces/msg/String` reuses the same message type and field (`string data`) as Chapter 4.1's `voice_command`, on a new topic (`robot_plan_step`). Jazzy init/shutdown idiom confirmed (no context-manager form, no `ExternalShutdownException`, no `AsyncNode`), matching every prior chapter. **Unconfirmed until run**: every console line, the exact plan Claude would return for "tidy the table" (shown in the chapter as traced by hand from the verified API shape, not observed), and the replanning loop's real behavior against a live model. |

---

## Module 2 review (T017a) — findings and outcome

Both reviewers ran together in fresh contexts across all four chapters plus the landing page, as
plan D4 requires.

| Reviewer | Blockers | Majors | Minors |
|---|---|---|---|
| `code-verifier` (whole module) | 1 | 6 | 12 |
| `consistency-checker` (whole module) | 0 | 6 | 8 |
| `code-verifier` (Chapter 2.2 re-review) | **0** | 3 | 5 |

**The blocker** was Chapter 2.2's `fall_probe.py`, described in its row above. It is worth naming
what caught it: not a reading of the chapter, but a two-step trace through shipping source —
`SceneBroadcaster.cc` sets no per-pose header, and `ros_gz`'s converter reads `child_frame_id` from
nowhere else. Chapter 2.1's Exercise 4 taught the same misconception and was rewritten with it.
The orchestrator re-verified both halves independently before any fix was applied.

**Confirmed sound and left alone.** Every numeric claim in the module was recomputed independently
by both reviewers and all of it checks out: total mass 34.700 kg; CoM at (0.002, 0.000, 0.007) in
`base_link` and 0.857 m above ground; support polygon x ∈ [−0.070, +0.150], y ∈ [−0.140, +0.140];
tipping angles 4.8° / 9.8° / 9.3°; τ = 0.30 s and 0.67 s to the backward limit; free fall 0.101 s
at 0.990 m/s; the six scaled inertia values; the sensor geometry; and every joint range in 2.4.
FR-017 holds — one humanoid, with `humanoid_cad_units.urdf` and `sensor_humanoid.urdf` as named,
justified, traceable derivatives. All 18 internal links resolve. No forward references to Modules
3 or 4; 2.3 withholds sensor fusion and 2.2 leaves balance control open, both as the catalog
requires.

**Spec change made rather than content cut.** All four chapters ran 3,300–4,200 prose words against
NFR-001's 1,200–2,500 band. On the user's decision the band was widened for Modules 2–4 to
1,200–4,500 rather than compressing the chapters or splitting the catalog, on the reasoning that a
chapter which must stand up an external toolchain before it can teach carries load-bearing setup
that Module 1 chapters do not. Applied consistently to `spec.md` (NFR-001 and SC-006), `plan.md`,
`data-model.md`, the `chapter-authoring` skill and the `consistency-checker` agent definition, with
the rationale recorded in the NFR so it is not re-litigated at the next review.

### Chapter 2.2 completion pass and targeted re-review — CLOSED

The writer applying Chapter 2.2's fixes had hit the session limit mid-edit, leaving four items open
(a stale bridge topic, a report timer that never received its clock argument, an un-updated
`physics_lab.sdf`, and the `/home/you/` placeholder). All four were completed, and because the
chapter's blocker had been touched, T017a's re-review rule was applied: `code-verifier` ran again on
**Chapter 2.2 only**.

**Result: 0 blockers, 3 majors, 5 minors.** The blocker is confirmed closed by a full end-to-end
source trace — see the Example 1 row above, which records the ten confirmed hops and the two
previously-open risks (per-pose stamps, and link poses being model-relative) that are now resolved.

Two of the three new majors were **introduced by the completion pass itself**, which is worth
recording rather than quietly fixing:

- Switching the report timer to a steady wall clock made the surrounding prose wrong in two ways.
  The claim that `use_sim_time` keeps "log timestamps" on simulated time is **false** — `rcutils`
  reads the system clock directly when formatting a log line (`logging.c`), and `rcl`'s
  `logging_rosout.c` stamps `/rosout` from the same system-time value, so no ROS parameter moves
  either. And the claim that `use_sim_time` "matters more here than in Chapter 2.1" became
  **inverted**: with all times taken from message stamps and the timer on a steady clock, nothing
  in this node's observable behaviour depends on the parameter, whereas Chapter 2.1's node printed
  `self.get_clock().now()` and genuinely did. Both were rewritten; the paragraph now explains the
  real division and notes that the bracketed log timestamp is always system time.
- The chapter still carried an execution-status italic on Example 2 while Example 1 had none. Under
  the module-wide M1 decision that status lives in this file, the remaining note was removed rather
  than a second one added — which also cleared a now-stale clause claiming the transcript's last
  line varies with the output path, no longer true after the placeholder fix.

Minors fixed in the same pass: the settle-window gate now compares with slack rather than depending
on exact float equality, and the environment section notes that `tf2_msgs` arrives with
`ros_gz_bridge`. **One correction to the reviewer's finding**: it reported that only 96 of 301
window checks passed under the old exact-equality gate. The orchestrator replayed faithful 100 Hz
sim-time stamps and **could not reproduce that** — old and new gates behaved identically (201/301,
`SETTLED` at the physically earliest 2.000 s), and that held from 50 Hz to 250 Hz. The slack is kept
as defensive hardening, not as a fix for a demonstrated failure.

**Two minors deliberately not fixed**, recorded as judgement calls:

1. `xml.etree.ElementTree` discards comments, so `scale_inertia.py`'s output copy silently loses the
   `<!-- Publish the model's own pose… -->` comment. The `<gazebo>` element itself survives, so
   Example 2's Gazebo run is unaffected; a reader diffing the two files sees one unexplained
   deletion. Cosmetic, and the chapter is long enough.
2. The reference-less `<gazebo>` extension mechanism is now explained in 2.2 (its first use) **and**
   again in `simulating-sensors.mdx`. Under FR-016 the second should become a cross-link. Left
   alone because this pass was scoped to Chapter 2.2 and must not edit another chapter — **carry to
   T019 or the next review touching 2.3.**

---

## Summary

| Metric | Count |
|---|---|
| Examples logged | 27 |
| `executed` | 6 |
| `pending-env` | 21 |
| `blocked` | 0 |
| Chapters `verified` | 1 of 14 |

**The Chapter 2.1 gap flagged in the previous revision of this file is now closed** — its three
rows are backfilled above, so the table no longer jumps from 1.4 to 2.2, and Module 2's pending set
is no longer undercounted.

Chapters 1.2 and 1.3 are **`drafted`**, not `verified`: both reviewed by both agents with all
blocker/major findings fixed and the site building, but no example in either has been executed.
Both become `verified` at T037.

Chapter 1.4 is **`drafted`**, and the closest of the four to done. Its two *chapter* examples are
both `executed` — the humanoid model itself and the loader script that reports its tree, together
covering every numeric claim the chapter makes about the robot. What remains is tooling around the
model, not the model: the official `urdfdom` parse, and the three-node visualization with its GUI.

**Module 1 is `drafted` in full** — four chapters plus the landing page, all four having passed both
reviewers with every blocker and major fixed, and the site building.

**Module 2 is now `drafted` in full.** All four chapters plus the landing page have been through
both reviewers, every blocker and major has been applied, Chapter 2.2 has additionally passed a
targeted re-review returning zero blockers, and the site builds. T017a is satisfied.

`drafted` is not `verified`, and Module 2 is further from `verified` than Module 1 was at the same
point. It holds exactly 1 of the project's 4 `executed` examples (`scale_inertia.py`, whose
transcript is now byte-exact end to end), and **not one line of its simulator-dependent output has
ever been observed** — every claim about what Gazebo does, what the probe prints, and how the two
falls differ rests on source reading and arithmetic. The blocker found this pass is the argument for
treating that as a real risk rather than a formality: it was invisible to prose review and to syntax
checking, and surfaced only by tracing two repositories to see which field actually gets populated.

**The cheapest path to Module 2's first real execution** is `joint_wave.py` (Chapter 2.4), which
needs only a plain ROS 2 Jazzy environment — no Gazebo, no Unity, no display. Everything else in the
module needs `ubuntu-24.04-gui` or `ubuntu-24.04-gpu`.

**Module 4 has begun** (T027–T028): the landing page and Chapter 4.1 are drafted. 4.1 splits its
code the same way Chapter 1.3 argued for — a plain-Python decision function versus a thin `rclpy`
adapter — and that split shows up directly in this file: its intent-mapping example is this
project's fifth `executed` row, while the Whisper-and-`rclpy` node stays `pending-env` for the same
reason nearly everything since Chapter 1.2 has: no ROS 2 on the authoring machine. A bounded,
time-boxed attempt to install `openai-whisper` under Python 3.12 was made and abandoned when the
`torch` wheel did not finish downloading — recorded on that row rather than silently retried.

**Module 4 now has two chapters drafted** (T027–T029). Chapter 4.2 repeats 4.1's split exactly: a
pure-Python plan validator with no ROS, no LLM SDK, and no network — this project's sixth `executed`
row, independently re-run against the code extracted from the shipped `.mdx` and confirmed
byte-identical to the authoring-time transcript — alongside an `rclpy` node that calls Claude and
dispatches to ROS 2, which stays `pending-env` for the same two reasons every ROS-dependent example
has: no ROS 2 on the authoring machine, and, per this chapter's own scope, no attempt made to call a
model API from here either. The `anthropic` SDK's tool-use shape was verified against
`platform.claude.com` rather than assumed, continuing the pattern Module 4 established with Whisper's
API in 4.1.

Update this table whenever rows are added or a status changes (tasks T012, T019, T026, T032, T037).
